namespace: dev-native

resources:
  - configmap.yaml
  - explorer.yaml
  - ingress.yml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: chain-configs
    files:
      - configs/gaia.json
      - configs/pstake.json
